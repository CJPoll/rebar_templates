%%% ============================================================================
%%% @author {{author}}
%%% @copyright {{year}}
%%% @doc 
%%% 
%%% @end
%%% @version 0.1
%%% ============================================================================
-module({{name}}).

% Example callback definition:
%-callback function_name(type(), type()) -> {ok, any()} | {error, any()}
